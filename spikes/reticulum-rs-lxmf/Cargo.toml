[package]
name = "reticulum-spike"
version = "0.1.0"
edition = "2021"

[dependencies]
# Use the transport crate directly - it has its own identity/hash types
reticulum-rs-transport = "0.2.0"
tokio = { version = "1", features = ["full"] }
rand_core = "0.6"
